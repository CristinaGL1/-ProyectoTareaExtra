
version: '3.8'


services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: Cristina
      POSTGRES_PASSWORD: contraseña
      POSTGRES_DB: ProyectoTareaExtra
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data


volumes:
  postgres-data:
